# Copy this file to .env and fill in values as needed.
# .env is gitignored — never commit secrets.

# Local node (default: Hardhat in-process)
RPC_URL=http://127.0.0.1:8545

# Testnet deployment
SEPOLIA_RPC_URL=
DEPLOYER_PRIVKEY=

# Optional: gas reporting
REPORT_GAS=false
